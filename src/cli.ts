#!/usr/bin/env bun\n\nimport { CLI } from 'bunli';\nimport { checkSystemFiles } from './commands/check';\nimport { checkFileStructure } from './commands/structure';\n\nconst cli = new CLI({\n  name: 'syschk',\n  version: '1.0.0',\n  description: 'System file checker CLI',\n});\n\ncli.command('check [path]', 'Check system files', checkSystemFiles)\n  .option('--verbose', 'Show detailed output')\n  .option('--json', 'Output as JSON')\n  .option('--deep', 'Deep scan of directories');\n\ncli.command('structure [path]', 'Show directory structure', checkFileStructure)\n  .option('--depth <n>', 'Maximum depth to traverse')\n  .option('--json', 'Output as JSON');\n\ncli.command('help', 'Show help', () => {\n  console.log(cli.help());\n});\n\nif (!process.argv.slice(2).length) {\n  console.log(cli.help());\n} else {\n  cli.parse(process.argv.slice(2));\n}